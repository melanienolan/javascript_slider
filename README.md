Code for a basic background image slider using JavaScript and jQuery.
Function uses 4 images and changes them in sequence every 4 seconds, or on click.
When background image changes, so does the text.
